# Model requirements

Docling supplies the layout backend and may download additional model artifacts. Record the package version, model-set identity, revision, artifact hashes, licenses, acquisition date, and cache preparation command. Never commit model binaries. Production and reproduced runs should use a verified model manifest and read-only model cache.
