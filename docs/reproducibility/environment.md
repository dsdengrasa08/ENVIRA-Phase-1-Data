# Environment

Project metadata defines compatible dependency ranges; files under `requirements/` capture validated environments. Install with Python 3.10–3.13 in an isolated environment. Published experiments must identify the exact constraint file and its hash. Colab is a supported interface, not the canonical environment definition.
