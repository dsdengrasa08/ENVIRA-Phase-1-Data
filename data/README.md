# Data

Only tiny, licensed, non-sensitive sample or synthetic inputs belong in `sample/`. Raw, external, interim, and processed research data are ignored. Document acquisition, licenses, corpus hashes, and split methodology in an experiment-specific manifest.
