# Notebooks

These notebooks are thin, output-free Colab interfaces. `pipeline_workflow_colab.ipynb` orchestrates and visualizes a pipeline run; `gradio_colab_launcher.ipynb` installs and launches the web application. Production logic belongs in the Python packages. Configure repository, input, storage, and profile values in the parameter cells rather than editing algorithm cells.
