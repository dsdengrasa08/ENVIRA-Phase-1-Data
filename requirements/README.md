# Validated environments

`constraints-regression.txt` pins the scientific regression environment. `constraints-colab.txt` is the release-specific Colab overlay and must be populated and validated before claiming a reproducible Colab release. Package `pyproject.toml` files remain authoritative for compatible dependency ranges.
